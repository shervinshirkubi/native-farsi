---
name: native-farsi
description: "نوشتن فارسی اصیل و طبیعی، نه فارسیِ ترجمه‌شده از انگلیسی. Use whenever ANY output is in Persian/Farsi: narration, podcast script, social content, article, caption, email, story, or translating English into Persian. Triggers: 'فارسی بنویس', 'نریشن', 'متن', 'اسکریپت', 'به فارسی', 'ترجمه کن', or any request whose deliverable is Persian text. Covers register detection, anti-calque rules, sentence construction, orthography, rhythm, and a gated draft-check-rewrite workflow."
---

# فارسی‌نویسِ اصیل

وقتی فارسی می‌نویسی، به فارسی **فکر کن**. جمله رو انگلیسی نچین و ترجمه نکن.
مشکلِ اصلیِ مدل‌ها در فارسی گرته‌برداریه: نحوِ انگلیسی با واژه‌های فارسی.

**اصلِ بنیادی: هیچ قانونی مطلق نیست.** هر قاعده وابسته به مخاطب و رجیستره. ساختاری که
تو کپشنِ اینستا مصنوعیه، تو مقاله‌ی توضیحی سالمه. قاعده‌ها با درجه علامت خوردن:
🔴 مطلق · 🟡 رجیستری · 🔵 تشخیصی.

## گامِ ۱: تشخیص (کاربر چیزی تنظیم نمی‌کنه)

**چه کاری؟**

| ورودی | mode |
|---|---|
| موضوع/کانسپت | تولید |
| متنِ انگلیسی | ترجمه ← `modes/translate-en-fa.md` |
| متنِ فارسی + «بهترش کن» | بازنویسی |

**چه رجیستری؟** از **مقصدِ متن** تشخیص بده:

```
قراره شنیده بشه؟ (پادکست، ویدیو، نریشن)
   ├─ آره → گفتاریِ شکسته
   └─ نه ↓
مخاطب کیه؟
   ├─ فرد/دوست (پیام، کپشن)  → صمیمیِ نوشتاری
   ├─ جمع، هدف: توضیح/آموزش   → نوشتاریِ توضیحی
   └─ جمع، هدف: اداری/حقوقی   → رسمی
```

**پیش‌فرضِ بی‌سیگنال: نوشتاریِ توضیحی.**

**قفلِ رجیستر:** تصمیم گرفتی، تا آخر همون بمون. قاطی‌کردن بزرگ‌ترین خطاست.

| | نوشتاری | گفتاری |
|---|---|---|
| فعل ربطی | است، هستند | ـه، ـن |
| فعل | می‌شود، می‌روند | می‌شه، می‌رن |
| ضمیر | آن، آن‌ها | اون، اونا |
| مفعول‌نما | را | رو |
| ملکی | کارتان، بقیه‌اش | کارتو، بقیه‌ش |

🔴 «می‌باشد» در هیچ رجیستری فارسیِ خوب نیست.

## گامِ ۲: چی لود کنم؟

| کِی | فایل |
|---|---|
| **همیشه** | `references/core.md` |
| متنِ بالای ۵۰ کلمه | `+ references/craft.md` |
| متنِ شنیداری | `+ references/spoken.md` + `voice-samples.md` |
| متنِ قصه‌دار (صحنه، شخصیت) | `+ references/storytelling.md` |
| ورودیِ انگلیسی | `+ references/modes/translate-en-fa.md` |
| مقصد: Word/PowerPoint/PDF/HTML | `+ references/documents.md` (فقط مکانیکِ فایل؛ رجیسترِ نثر جدا لود شه) |

**همه رو با هم لود نکن.** برای یه کپشن فقط `core.md` کافیه.

## گامِ ۳: ورک‌فلو، با گیت

| طول | ورک‌فلو |
|---|---|
| زیرِ ۵۰ کلمه | پیش‌نویس ← چکِ مکانیکیِ `core` ← تحویل |
| ۵۰ تا ۳۰۰ | + چکِ `craft` ← بازنویسیِ موارد |
| بالای ۳۰۰، نریشن، ترجمه | چهار دروازه (پایین) |

> ⚠️ **چک ساکت انجام می‌شه.** لیستِ تخلف‌ها رو به‌صورت متنِ جدا تولید نکن مگه کاربر
> صریح خواسته باشه. فقط اصلاح کن و متنِ نهایی رو بده.

### چهار دروازه (فقط متنِ بلند)

دروازه‌ای رد شد، بازنویسی کن و **از دروازه‌ی یک دوباره** شروع کن.

۱. **تکرار:** اگه بخشی از یه مجموعه‌ست، با قبلی‌ها diff بزن. هم جمله باید نو باشه، هم **فرم**.
۲. **جمله و ساختار:** `craft.md`. (رواییه؟ `storytelling.md`)
۳. **گرته و رجیستر:** `core.md`. رجیستر از اول تا آخر یکدست؟
۴. **صحت:** تناقضِ داخلی؟ هم‌خوانیِ فاعل/فعل؟ ادعای بی‌منبع؟ هر جمله یک‌نفس؟

**سوالِ آخر:** «جمله‌ای هست که بوی ترجمه بده؟» یکی هم مشکوک بود، آماده نیست.

## قاعده‌ی همیشگی: فعل، قلبِ جمله‌ی فارسیه

انگلیسی اسم‌محوره، فارسی فعل‌محور.

- ❌ «بررسیِ این موضوع انجام شد» ✅ «این موضوع رو بررسی کردیم»
- ❌ «این تصمیم توسط مربی گرفته شد» ✅ «مربی این تصمیم رو گرفت»

🔵 «وجود داره» فی‌نفسه مشکلی نداره؛ مشکل وقتیه که *هر* جمله رو باهاش می‌سازی.
مجهول فقط وقتی فاعل واقعاً مهم نیست. «توسط» تقریباً همیشه نشونه‌ی ترجمه‌ست.

## فایل‌ها

```
references/
├── core.md            هسته: کالک واژگانی · یکدستیِ رجیستر · رسم‌الخط   [همیشه]
├── craft.md           جمله‌سازی · الگوهای AI · ریتم                    [>۵۰ کلمه]
├── spoken.md          شکسته‌نویسی · تکیه‌کلام · ریتمِ شنیداری           [شنیداری]
├── voice-samples.md   متنِ واقعیِ گفتاری                               [شنیداری]
├── storytelling.md    روایت و توصیف                                    [قصه‌دار]
├── documents.md        docx/pptx/PDF/HTMLِ راست‌چین + وریفای          [خروجی = فایل]
└── modes/
    └── translate-en-fa.md                                             [ترجمه]

scripts/
├── rhythm_check.py     سنجه‌ی کمّیِ ریتم (CV)                        [متنِ بلند/روایی]
├── verify_docx.py       چکِ RTLِ فایلِ Word                          [بعدِ ساختِ docx]
├── verify_pdf.py        چکِ فونتِ fallback و صفحه‌ی خالی              [بعدِ ساختِ PDF]
└── install_fonts.sh     نصبِ Vazirmatn/Lalezar قبلِ هر تبدیلِ PDF     [قبلِ docx/pptx→PDF]
```
